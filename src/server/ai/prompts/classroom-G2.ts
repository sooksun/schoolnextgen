/**
 * @deprecated Use classroom-lower-primary.ts instead.
 * Kept as a re-export for back-compat (e.g., the seed script reads this file
 * via regex parsing — Phase 2 cleanup should remove the regex pattern).
 */
export { CLASSROOM_PERSONA_LOWER_PRIMARY as CLASSROOM_PERSONA_G2 } from './classroom-lower-primary'
